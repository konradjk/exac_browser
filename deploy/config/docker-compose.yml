version: '2'

services:
  exacserve:
    build:
      context: .
      dockerfile: docker/exacserve.dockerfile
    image: macarthurlab/exacserve
    ports:
      - "80:80"
    networks:
      - exac-network
  exacload:
    build:
      context: .
      dockerfile: docker/exacload.dockerfile
    image: macarthurlab/exacload
    networks:
      - exac-network
    depends_on:
      - mongodb
  mongodb:
    image: mongo
    networks:
      - exac-network

networks:
  exac-network:
    driver: bridge
